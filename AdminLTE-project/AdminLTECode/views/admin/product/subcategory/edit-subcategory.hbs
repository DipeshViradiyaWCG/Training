{{> header}}
{{> sidebar}}

    <div class="content-wrapper">

        <section class="content">   
            <div class="container-fluid">
                
                <div class="row justify-content-evenly mt-3">
                    <div class="col-6">
                        <h3>Edit Sub Category</h3>
                        <hr>
                        <form method="POST">
                            <div class="mb-3">
                                <label for="firstname" class="form-label">Subcategory Name</label>
                                <input type="text" class="form-control" id="firstname" name="subcategoryname" value="{{subcategoryname}}" aria-describedby="emailHelp">
                            </div>

                            <div class="mb-3">
                                <div class="">
                                    <label class="" for="inputGroupSelect01">Category : </label>
                                </div>
                                <select class="custom-select" name="_category" id="inputGroupSelect01">
                                    {{#each categories}}
                                    <option value="{{categoryname}}">{{categoryname}}</option>
                                    {{/each}}
                                </select>
                            </div>
                            
                            <button type="submit" class="btn btn-primary">Submit</button>
                        </form>
                    </div>
                </div>

            </div>
        </section>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <script>
        let selectedCategory = '{{{selectedCategory}}}'
        $().ready(function(){
            $('#inputGroupSelect01 option[value=' + selectedCategory + ']').attr("selected", true);
        });
    </script>

{{> footer}}