{{fetched_user_data.length}} Records found...
<table border="1">
    <tr>
        <th>Name</th>
        <th>Mobile No.</th>
        <th>Action</th>
    </tr>
    {{#each fetched_user_data}}
    <tr>
        <td>{{user_name}}</td>
        <td>{{user_mobile}}</td>
        <td><a href="edit/{{_id}}">Edit</a> || <a href="delete/{{_id}}">Delete</a></td>
    </tr>
    {{/each}}
    
</table>